import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minifactory.app',
  appName: 'minifactory',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
